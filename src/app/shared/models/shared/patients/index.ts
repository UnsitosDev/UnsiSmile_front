export interface patientsTableData {
  patientID: number; // Agregamos esta propiedad para corregir el error
  nombres: string;
  apellidos: string;
  correo: string;
  curp: string;
  telefono: string;
  fechaNacimiento: string;
  estatus: string;
  
}